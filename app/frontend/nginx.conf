server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # without this, refreshing any react route gives a 404
        try_files $uri $uri/ /index.html;
    }
}